def make_shirt(text_shirt = '', size_shirt = 'large'): ##Cuando se agrega un valor por default a un parámetro, se tiene
                                                       ## Se tiene que reordenar el acomodo de la función.
    print(f"I have a {size_shirt} shirt with the {text_shirt} message printed on it.")

make_shirt('"I love Python"')
